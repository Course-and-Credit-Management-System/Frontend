const fs = require('fs'); const file = fs.readFileSync('C:/Frontend/Frontend/pages/StudentMessages.tsx', 'utf8'); const regex = /t\(['](.+?)[']\)/g; const strings = new Set(); let match; while ((match = regex.exec(file)) !== null) { strings.add(match[1]); } const enPath = 'C:/Frontend/Frontend/public/locales/en/translation.json'; const myPath = 'C:/Frontend/Frontend/public/locales/my/translation.json'; const en = JSON.parse(fs.readFileSync(enPath, 'utf8')); const my = JSON.parse(fs.readFileSync(myPath, 'utf8')); strings.forEach(s = if (!en[s]) en[s] = s; if (!my[s]) my[s] = s; }); fs.writeFileSync(enPath, JSON.stringify(en, null, 2)); fs.writeFileSync(myPath, JSON.stringify(my, null, 2)); console.log('# of keys added: ', strings.size); 
